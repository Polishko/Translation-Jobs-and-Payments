/*This table contains the job purchase orders and is used to track whether project orders are prepared on a timely manner for delivered projects. Purchase orders are needed by translators for proforma preparation.  
The procedure sp_add_purchase_order is used to populate the table when a project is created (See functions, procedures and triggers file)*/


-- calling the procedure to populate the table with new purchase order:

CALL fn_add_purchase_order('156_BCP Manual TR', 'PTRIC084732');

